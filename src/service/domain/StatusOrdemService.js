import Rest from '../Rest'

/**
 * @typedef {StatusOrdemService}
 */
export default class StatusOrdemService extends Rest {
    /**
     * @type {String}
     */
    static resource = '/statusordem'
}